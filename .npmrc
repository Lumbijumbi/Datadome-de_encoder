# npm configuration for datadome-de-encoder

# Ensure package is published as public
access=public

# Use new package-lock format
lockfile-version=3

# Strict engine checking
engine-strict=true

# Save exact versions in package.json
save-exact=false

# Automatically save installed packages to package.json
save=true
